import React from 'react';
import ReactDOM from 'react-dom/client';
import ZillowBuyability from './zillow-buyability';

const root = document.getElementById('root');
if (root) {
  ReactDOM.createRoot(root).render(
    <React.StrictMode>
      <ZillowBuyability />
    </React.StrictMode>
  );
}









