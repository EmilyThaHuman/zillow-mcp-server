import React from 'react';
import ReactDOM from 'react-dom/client';
import ZillowAreas from './zillow-areas';

const root = document.getElementById('root');
if (root) {
  ReactDOM.createRoot(root).render(
    <React.StrictMode>
      <ZillowAreas />
    </React.StrictMode>
  );
}









